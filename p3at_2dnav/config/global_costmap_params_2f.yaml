global_costmap:
  global_frame: /map
  robot_base_frame: base_link
  update_frequency: 2.0
  publish_frequency: 10.0
  static_map: true            # If you aren't using an existing map or map server, set the static_map parameter to false.
  #cost_scaling_factor: 5.0
